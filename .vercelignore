# Серверна частина (WordPress mu-plugin) — не деплоїмо на статику Vercel
backend/
# Бекапи/staging (містять ПІІ) — і так у .gitignore, дублюємо про всяк
# Провідний "!" екрануємо (інакше рядок сприймається як заперечення і папка потрапляє в деплой)
\!!!backup/
staging/
docs/
